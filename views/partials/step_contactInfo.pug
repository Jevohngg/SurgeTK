// =======================================================================
// step_contactInfo.pug   (index = 1 in modal)
// =======================================================================
div#contact-info-step
  // same outer wrapper & class names as Guardrails
  div.buckets-guide-container
    section.guide(role='region' aria-labelledby='contact-info__title')
      // ───────── Title & intro ─────────
      header.guide__header
        h2#contact-info__title.guide__title Prepare Your Spreadsheet
        p.guide__intro
          | Before importing your 
          strong Contact
          |  data, ensure your spreadsheet includes the columns below. Additional fields are available during the mapping step.

      // Legend: required vs optional
      .guide__legend(aria-label='Field key')
        span.badge.badge--required Required
        span.badge.badge--optional Optional

      // ───────── Checklist ─────────
      ol.guide-steps
        li.guide-step
          .guide-step__head
            span.field-name Household ID
            span.badge.badge--required Required
          .guide-step__body
            p Uniquely identifies a household.

        li.guide-step
          .guide-step__head
            span.field-name Client ID
            span.badge.badge--required Required
          .guide-step__body
            p Uniquely identifies the client.

        li.guide-step
          .guide-step__head
            span.field-name Client Name
            span.badge.badge--required Required
          .guide-step__body
            p Use 
              code lastName, firstName
              |  format (e.g., 
              code Doe, John
              | ).

      // ───────── Best-practice tips ─────────
      section.guide__notes(aria-label='Tips for preparing your file')
        h3.guide__subheading Tips
        ul.guide__list
          li Keep Household and Client IDs consistent for future uploads.
          li Source data from any CRM, or start with the template below.

      // ───────── Download + Learn-More links (exact links preserved) ─────────
      section.guide__actions
        a.action.action--primary.import-action-item(
          href='/downloads/contact-import-template.xlsx'
          download
          role='button'
        )
          i.fas.fa-file-download.action__icon(aria-hidden='true')
          span Download the Contact Import template

        a.action.action--ghost.import-action-item.learn-more-btn(      
          href='#'
          data-video-id='76979871'                           
          data-bs-toggle='modal'
          data-bs-target='#learnMoreVideoModal'
          role='button'
        )
          i.fas.fa-play-circle.action__icon(aria-hidden='true')
          span Watch an instructional video (2 min)

      // ───────── Example header row ─────────
      section.guide__example
        h3.example-title Example header row
        pre.example-code
          code Household ID | Client ID | Client Name | Gender | Marginal Tax Bracket % | Lead Advisor | Occupation | Retirement Date | Employer | Email | Phone Number | Deceased Living
