// step_bucketsInfo.pug

div#buckets-info-step
  div.buckets-guide-container
    section.guide(role='region' aria-labelledby='buckets-info__title')
      // ───────── Title & intro ─────────
      header.guide__header
        h2#buckets-info__title.guide__title Prepare Your Spreadsheet
        p.guide__intro
          | Before importing your 
          strong Buckets Report
          |  data, ensure your spreadsheet includes the following columns.

      // Legend: required vs optional
      .guide__legend(aria-label='Field key')
        span.badge.badge--required Required
        span.badge.badge--optional Optional

      // ───────── Fields checklist ─────────
      ol.guide-steps
        li.guide-step
          .guide-step__head
            span.field-name Client ID
            span.badge.badge--optional Optional (required for new accounts)
          .guide-step__body
            p A unique identifier for each client.

        li.guide-step
          .guide-step__head
            span.field-name Account Number
            span.badge.badge--required Required
          .guide-step__body
            p The account associated with the client.

        li.guide-step
          .guide-step__head
            span.field-name Withdraw Amount
            span.badge.badge--required Required
          .guide-step__body
            p The recurring withdrawal dollar amount.

        li.guide-step
          .guide-step__head
            span.field-name Withdraw Frequency
            span.badge.badge--required Required
          .guide-step__body
            p For example: 
              code Monthly
              | , 
              code Quarterly
              | , 
              code Annually
              | .

        li.guide-step
          .guide-step__head
            span.field-name Asset Allocation
            span.badge.badge--required Required
          .guide-step__body
            p Provide each percentage so that the total equals 
              code 100%
              |  for every account.
            ul.guide__list.a-allocation-guide
              li 
                code Cash %
              li 
                code Income %
              li 
                code Annuities %
              li 
                code Growth %

      // ───────── Best-practice tips ─────────
      section.guide__notes(aria-label='Tips for preparing your file')
        h3.guide__subheading Tips
        ul.guide__list
          li All listed fields are required for a complete and accurate Buckets report.
          li Ensure each account’s asset allocation sums to 
            code 100%
            | .
          li Source data from any CRM, or start with the template below.

      // ───────── Download + Learn-More links (exact links preserved) ─────────
      section.guide__actions
        a.action.action--primary.import-action-item(
          href='/downloads/buckets-account-import-template.xlsx'
          download
          role='button'
        )
          i.fas.fa-file-download.action__icon(aria-hidden='true')
          span Download the Buckets Import template

        a.action.action--ghost.import-action-item.learn-more-btn(
          href='#'
          data-video-id='76979871'
          data-bs-toggle='modal'
          data-bs-target='#learnMoreVideoModal'
          role='button'
        )
          i.fas.fa-play-circle.action__icon(aria-hidden='true')
          span Watch an instructional video (2 min)

      // ───────── Example header row ─────────
      section.guide__example
        h3.example-title Example header row
        pre.example-code
          code Client ID | Account Number | Withdraw Amount | Withdraw Frequency | Cash % | Income % | Annuities % | Growth %
